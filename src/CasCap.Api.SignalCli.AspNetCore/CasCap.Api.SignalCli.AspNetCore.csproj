<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <Description>ASP.NET Core integration for CasCap.Api.SignalCli — a versioned, read-only MVC controller exposing signal-cli account, contact, group, device, identity, attachment and sticker-pack queries.</Description>
    <PackageTags>signal,signal-cli,messaging,aspnetcore,mvc,webapi</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="Asp.Versioning.Mvc" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CasCap.Api.SignalCli\CasCap.Api.SignalCli.csproj" />
  </ItemGroup>

</Project>
