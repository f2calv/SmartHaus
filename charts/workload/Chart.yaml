apiVersion: v2
name: workload
description: Generic, reusable Helm chart for deploying Kubernetes workloads (Deployment, DaemonSet, StatefulSet, CronJob, KEDA ScaledObject/ScaledJob).
type: application
version: 0.1.0
appVersion: "1.16.0"
